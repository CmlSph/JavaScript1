var x = 7; 
x = x % 3;
console.log(x);

/*The answer is 1. The remainder operator (%) returns the remainder left over 
when one operand is divided by a second operand. It always takes the sign of the dividend.
When we divide 7 by 3 the remainder is 1*/

var y = 21;
y = y % 4;
console.log(y);

/*The answer is 1. The remainder operator (%) returns the remainder left over 
when one operand is divided by a second operand. It always takes the sign of the dividend.
When we divide 21 by 4 the remainder is 1*/

var z = 13;
z = z % 2;
console.log(z);

/*The answer is 1. The remainder operator (%) returns the remainder left over 
when one operand is divided by a second operand. It always takes the sign of the dividend.
When we divide 13 by 2 the remainder is 1*/